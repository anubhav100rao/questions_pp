#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "local/debug.cpp"
#else
#define debug(...) 42
#endif

#define dbg(x) cerr<<#x<<" = "<<x<<endl
#define int long long


signed main() {

    vector<vector<vector<int>>> adj = {
        {{2, 500}, {3, 100}},
        {{4, 50}, {6, 40}},
        {{4, 10}}
    };
 
    return 0;
}